import 'package:flutter/material.dart';

class BooksCopy extends StatelessWidget {
  final String pdfLink = 'assets/pdfs/indexed_pdf.pdf';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Books Copy"),
      ),
      body: Container(),
    );
  }
}
